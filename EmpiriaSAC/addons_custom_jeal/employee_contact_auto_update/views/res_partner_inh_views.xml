<?xml version="1.0" encoding="UTF-8"?>
<odoo>
  <data noupdate="0">
    <record id="view_partner_form_employee_extension" model="ir.ui.view">
      <field name="name">res.partner.form.employee.extension</field>
      <field name="model">res.partner</field>
      <field name="inherit_id" ref="base.view_partner_form" />
      <field name="arch" type="xml">
        <!-- Inserta los campos personalizados -->
        <xpath expr="//group/group/field[@name='category_id']" position="after">
          <field name="is_employee" readonly="1" widget="boolean_toggle" />
          <!-- <field name="associated_employee_id" attrs="{'invisible': [('is_employee', '=', False)]}" /> -->
        </xpath>
      </field>
    </record>
  </data>
</odoo>
